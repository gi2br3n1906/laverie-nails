<x-layouts.app title="Detail Produk Laverie">
    <div class="mx-auto max-w-5xl"><div class="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><a class="text-sm font-semibold text-stone-900" href="{{ route('admin.catalogs.index') }}">← Katalog Laverie</a><h1 class="mt-4 font-serif text-5xl font-semibold">{{ $catalog->title }}</h1><p class="mt-3 text-stone-500">Size {{ $catalog->size->value }} • {{ $catalog->is_active ? 'Tampil di toko' : 'Disembunyikan' }}</p></div><a class="rounded-full border border-stone-300 bg-white px-5 py-3 text-center font-semibold text-stone-800" href="{{ route('admin.catalogs.edit', $catalog) }}">Edit produk</a></div><div class="mt-8 grid gap-5 sm:grid-cols-2">@foreach ($catalog->images as $image)<img class="aspect-[4/3] w-full rounded-3xl bg-stone-100 object-cover" src="{{ Storage::disk('public')->url($image) }}" alt="{{ $catalog->title }}">@endforeach</div><div class="mt-8 rounded-3xl bg-white p-6"><p class="leading-7 text-stone-600">{{ $catalog->description }}</p><p class="mt-5 text-2xl font-bold text-stone-800">Rp {{ number_format((float) $catalog->price, 0, ',', '.') }}</p></div><form class="mt-6" method="POST" action="{{ route('admin.catalogs.destroy', $catalog) }}">@csrf @method('DELETE')<button class="rounded-full border border-stone-300 bg-stone-100 px-5 py-3 font-semibold text-stone-800" type="submit">Hapus produk</button></form></div>
</x-layouts.app>