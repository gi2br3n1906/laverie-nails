<x-layouts.app title="Kelola Kategori">
    <div class="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-bold uppercase tracking-[0.2em] text-stone-600">Shop by Style</p><h1 class="mt-3 font-serif text-5xl font-semibold">Kelola Kategori</h1><p class="mt-4 text-stone-600">Kelola kategori editorial untuk tampilan storefront.</p></div><a class="rounded-full bg-stone-900 px-6 py-3 text-center font-semibold text-white" href="{{ route('admin.categories.create') }}">Tambah kategori</a></div>
    @if (session('status'))<div class="mt-6 rounded-2xl bg-emerald-50 px-5 py-4 text-emerald-800">{{ session('status') }}</div>@endif
    <div class="mt-10 overflow-hidden rounded-3xl border border-stone-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-stone-100 text-left text-sm"><thead class="bg-stone-100 text-xs uppercase tracking-wider text-stone-800"><tr><th class="px-5 py-4">Kategori</th><th class="px-5 py-4">Slug</th><th class="px-5 py-4">Produk</th><th class="px-5 py-4">Aksi</th></tr></thead><tbody class="divide-y divide-stone-100">@forelse ($categories as $category)<tr><td class="px-5 py-4 font-semibold">{{ $category->name }}</td><td class="px-5 py-4 text-stone-500">{{ $category->slug }}</td><td class="px-5 py-4">{{ $category->products_count }}</td><td class="px-5 py-4"><div class="flex gap-4"><a class="font-semibold" href="{{ route('admin.categories.edit', $category) }}">Edit</a><form method="POST" action="{{ route('admin.categories.destroy', $category) }}">@csrf @method('DELETE')<button class="font-semibold text-stone-600" type="submit">Delete</button></form></div></td></tr>@empty<tr><td class="px-5 py-8 text-center text-stone-500" colspan="4">Belum ada kategori.</td></tr>@endforelse</tbody></table></div></div>
</x-layouts.app>