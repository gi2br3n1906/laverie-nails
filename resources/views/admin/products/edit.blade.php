<x-layouts.app title="Edit Produk">
    <div class="mx-auto max-w-3xl"><a class="text-sm font-semibold" href="{{ route('admin.products.index') }}">← Kelola Produk</a><h1 class="mt-4 font-serif text-5xl font-semibold">Edit Produk</h1>@if (session('status'))<div class="mt-6 rounded-2xl bg-emerald-50 px-5 py-4 text-emerald-800">{{ session('status') }}</div>@endif<form class="mt-8 rounded-3xl border border-stone-200 bg-white p-6 shadow-sm sm:p-8" method="POST" action="{{ route('admin.products.update', $product) }}" enctype="multipart/form-data">@csrf @method('PUT') <x-admin.product-form :product="$product" :categories="$categories" /><div class="mt-8 flex justify-end"><button class="rounded-full bg-stone-900 px-7 py-3 font-semibold text-white" type="submit">Perbarui produk</button></div></form>@foreach ($product->images as $image)<form id="delete-product-image-{{ $image->id }}" method="POST" action="{{ route('admin.products.images.destroy', [$product, $image]) }}">@csrf @method('DELETE')</form>@endforeach</div>
</x-layouts.app>