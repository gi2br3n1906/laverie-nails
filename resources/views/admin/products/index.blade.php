<x-layouts.app title="Kelola Produk">
    <div class="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-bold uppercase tracking-[0.2em] text-stone-600">Editorial storefront</p><h1 class="mt-3 font-serif text-5xl font-semibold">Kelola Produk</h1><p class="mt-4 text-stone-600">Kelola koleksi produk dan gambar untuk homepage.</p></div><a class="rounded-full bg-stone-900 px-6 py-3 text-center font-semibold text-white" href="{{ route('admin.products.create') }}">Tambah produk</a></div>
    @if (session('status'))<div class="mt-6 rounded-2xl bg-emerald-50 px-5 py-4 text-emerald-800">{{ session('status') }}</div>@endif
    <div class="mt-10 overflow-hidden rounded-3xl border border-stone-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-stone-100 text-left text-sm"><thead class="bg-stone-100 text-xs uppercase tracking-wider text-stone-800"><tr><th class="px-5 py-4">Produk</th><th class="px-5 py-4">Kategori</th><th class="px-5 py-4">Harga</th><th class="px-5 py-4">Stok</th><th class="px-5 py-4">Status</th><th class="px-5 py-4">Aksi</th></tr></thead><tbody class="divide-y divide-stone-100">@forelse ($products as $product)<tr><td class="px-5 py-4"><div class="flex items-center gap-3">@if ($product->primaryImage)<img class="size-14 rounded-xl object-cover" src="{{ Storage::disk('public')->url($product->primaryImage->image_path) }}" alt="{{ $product->name }}">@else<span class="grid size-14 place-items-center rounded-xl bg-stone-100 text-xs text-stone-400">No image</span>@endif<span class="font-semibold">{{ $product->name }}</span></div></td><td class="px-5 py-4">{{ $product->category->name }}</td><td class="px-5 py-4">Rp {{ number_format((float) $product->price, 0, ',', '.') }}</td><td class="px-5 py-4">{{ $product->stock }}</td><td class="px-5 py-4">{{ $product->is_active ? 'Aktif' : 'Nonaktif' }}</td><td class="px-5 py-4"><div class="flex gap-4"><a class="font-semibold" href="{{ route('admin.products.edit', $product) }}">Edit</a><form method="POST" action="{{ route('admin.products.destroy', $product) }}">@csrf @method('DELETE')<button class="font-semibold text-stone-600" type="submit">Delete</button></form></div></td></tr>@empty<tr><td class="px-5 py-8 text-center text-stone-500" colspan="6">Belum ada produk editorial.</td></tr>@endforelse</tbody></table></div></div>
</x-layouts.app>